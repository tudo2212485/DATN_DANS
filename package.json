{
  "name": "agroforecast-root",
  "version": "1.0.0",
  "description": "AgroForecast Root Launcher",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend start"
  }
}
